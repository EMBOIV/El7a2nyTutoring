# SMTP settings for booking email delivery
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password

# Sender email address shown in outgoing booking messages
FROM_EMAIL=no-reply@el7a2ny.com
